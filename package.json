{
  "name": "bopup-ledger",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:js && npm run test:py",
    "test:js": "node --test tests/*.test.mjs",
    "test:py": "python -m unittest discover -s tests -p \"test_*.py\"",
    "audit:migrate": "python scripts/migrate_portfolio_v5.py",
    "audit:snapshot": "node scripts/audit_snapshot.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
