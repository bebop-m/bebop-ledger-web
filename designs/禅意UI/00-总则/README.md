# 禅意UI · 素禅/墨禅改版施工总包

> **本目录是唯一权威**：18 个界面文件夹，每个含 定稿图.html、施工方案.md、验收标准.md。任何 Agent 依此可一比一实现，无需其他上下文。
> 定稿图与施工方案文字冲突时：**视觉构图以定稿图为准，功能取舍以施工方案为准**。
> 夜间态参照物见本目录 `夜间色板.html`（各页定稿图只画了素禅日间）。
> 落地前先读下面「定稿图怎么落地」——定稿图不是像素级照搬的对象，直接抄绝对值会错。
> 旧目录 designs/home-color-directions/ 为历史归档，勿作施工依据。

## 当前状态（2026-07-25）
- 设计已全部经用户逐页确认定稿。
- **A / B / C / D / E 五簇已施工完成并上线**：01-首页、02–05 持仓四件套、06–08 股息三件套、
  09–12 收益四件套、13–16 资金四件套。线上 main 是「除公司基本面外全部为新版」的
  混搭状态，按簇推进中。
- 剩余 F（17–18 基本面）一簇。
- 施工在 `zen-redesign` 分支，一簇一做、一簇一验、一簇上线。
- 首次施工（回滚那版）的功能成果仍在历史里可复用（币种折算/XIRR 删除/饼图/分享卡/速览/估值），但**布局层一律按定稿图重排**。

---

## 定稿图怎么落地（施工前必读）

定稿图是**一张 390px 宽画布上的静态快照**，而应用跑在 **iPhone 15 Pro Max（430×932）的单屏 PWA** 里。三处系统性差异，照搬绝对值必错：

### 1. 尺寸要等比缩放，不能照搬
定稿图的 47px / 214px / 58px 都是 390 画布下的值。直接写死，在 430 屏上等于把整套设计缩小 9%（首页因此返工过一次）。

**做法**：每个尺寸折算成 vw，再用 clamp 兜住两端。
```css
/* 定稿值 N（基于 390 画布）→ */
font-size: clamp(N*0.92 px, N/390*100 vw, N*1.35 px);
/* 例：47px → clamp(43.24px, 12.0513vw, 63.45px) */
```
- 390 屏精确回到定稿值，430 屏等比放大 1.103，窄屏（SE）不小于 0.92 倍，桌面不超过 1.35 倍。
- **没有全局缩放变量可用**：CSS 的 `calc()` 里长度不能除以长度得到纯数，`calc(100vw / 390)` 得到的仍是长度，乘上 px 会变成非法的 px² 让整条规则失效。只能每个尺寸各自折算。
- 媒体查询里的断点（如 `max-height: 760px`）是判据不是尺寸，**不要缩放**。

### 2. 真机有状态栏，定稿图没有
`apple-mobile-web-app-status-bar-style` 是 `default`（不透明），页面**从状态栏下方开始**，此时 `env(safe-area-inset-top)` 返回 0。定稿图顶部那 30px 是画在无状态栏画布上的，真机上状态栏已占 59pt 提供了视觉呼吸，**再叠 30px 就是重复留白**（首页因此返工过一次）。

**做法**：顶部设计留白取定稿值的一半左右（首页用 14px），并保留 `max(设计值, env(safe-area-inset-*))` 以防日后改用 `black-translucent` 需要真正避让。
```css
padding-top: max(clamp(...), env(safe-area-inset-top, 0px));
```

### 3. 边距只由页面根元素一处控制
`body` 有 `padding: 14px 22px`、`.app-shell` 有 `max-width: 386px`，与页面自身的内距叠加后，430 屏上内容区只剩 294px（应为 378px）。**每个页面在自己的 `body[data-active-page="X"]` 作用域内让 body 与 app-shell 交出 padding 和 max-width**，边距由页面根元素独占。

### 4. 定稿图里有些效果是浏览器默认行为的产物
典型：`.hero-label` 是 inline `<span>`，行盒被容器 16px 默认字号的 strut 撑到 23px（元素自身只有 14.5px）；
`<p>` 保留浏览器默认 margin。落地若改成 block，竖向节奏会变紧。

**但不要为此逐像素追平**。判据是「并排看不出差别」，不是数值相等——
只有在**目检发现某块节奏明显不对**时，才去定稿图里跑一次 `getComputedStyle` 拿真实数字来对齐；
没看出问题就不必量。追到 0.3px 级别属于浪费。

---

## Token（styles.css 权威层）
素禅（日间默认）：--bg:#faf8f3; --ink:#3b362e; --muted:#a89d86; --hint:#c2b9a6; --label:#b0a78f; --gold:#c19a45; --gold-soft:#dcc492; --track:#eae4d4; --up:#bf5a42; --down:#6a8b74; 遮罩 rgba(59,54,46,.28)
墨禅（夜间，prefers-color-scheme 自动）：--bg:#23201b; --ink:#ece6d8; --muted:#8f887a; --hint:#5f594c; --label:#8f887a; --gold:#d3a84f(进度线加 0 0 12px rgba(211,168,79,.45) 微光); --gold-soft:#b9974a; --track:#3a352c; --up:#e07a5f; --down:#7fae8e; 遮罩 rgba(0,0,0,.45)

### 图表色（各页定稿图只画了日间，夜间以 `夜间色板.html` 为准）
定稿图里折线/圆点/饼图分段写的是硬编码 hex，**施工时一律改用下表 token，不许写死**。夜间值按对比度反解得到（暗底细线有光晕衰减，线条类目标 3.4–5.2:1、点类 6.7–7.3:1，均高于日间；饼图六阶保证相邻 ≥1.25:1 可辨）。

| token | 用在哪 | 素禅日间 | 墨禅夜间 |
| --- | --- | --- | --- |
| --chart-line | 资金收益率趋势线 | #b5a88f | #8f8266 |
| --chart-gold | 股息收益率线、每股分红线 | #dcc492 | #a98e50 |
| --chart-line-soft | EPS 线 | #cfc4ad | #8a7f66 |
| --chart-gold-dot | 分红线/股息线数据点 | #c9a558 | #d3a84f |
| --chart-ink-dot | EPS 点、资金收益率点 | #8a7d69 | #b3a68c |
| --chart-growth | 公式三段线「增长」段 | #b3a68c | #7d7360 |
| --donut-1 … 6 | 持仓饼图六阶 | #c9a558 / #dcc492 / #b3a68c / #cfc4ad / #e2d9c4 / #efe9da | #d3a84f / #b9974a / #8f8262 / #6f6653 / #514b3d / #3a352c |

SVG 内的文字 fill 同样走 token（--ink / --muted / --hint），定稿图里的 `fill:#3b362e` 一类是日间快照值，不可照抄。
**例外**：11-分享卡固定素禅日间导出（卡底 #fffdf8），**不随夜间变色**，两态渲染应完全一致。

---

## 覆盖旧层（styles.css 有 1 万行历史层）

zen 层写在文件末尾靠后来居上取胜，但**只写「想改的属性」不够**——没写的属性会继续用旧层的值，
且往往在特定屏宽/屏高下才显形（首页踩过 7 处：display/flex/height/min-height/font-size/border/::before）。

**做法**：新写区块时对每个元素显式声明 `display / flex / height / min-height / padding / margin / border / font-size`，
伪元素显式 `content: none; display: none`。宁可多写几行。

旧层还藏着 **id 选择器**（如 `#holdingsSortLabel { display: none }`），权重压过类选择器，
zen 层写得再靠后也赢不了——元素整个不显示时，先拿它的 id 去旧层里搜
（B 簇排序键就是这么丢的，机检还一路全绿）。

**不必自己写脚本排查**——`npm run verify` 已自动扫描描边、伪元素装饰、按钮底托、居中失效、横贯分隔线。
照着它报的修即可。

## 全局纪律（每个界面都受约束）
- 结构靠排版不靠容器：**无卡片边框、无图标底托、无 chevron、无分隔 rule**；分区只用留白。
- 字阶（均为 390 画布下的定稿值，落地按上面的等比规则缩放）：首页 hero 47px/600；内页 hero 38px/600 居中；节标 10px/600/字距 4-5px var(--label)；页名 11px/字距 5px 居中；数字 tabular-nums。
- 页头统一 grid(56px/1fr/56px)：左裸返回箭头 18px，中页名，右功能位。
- 金色=唯一记号色（股息+选中）；选中态=4px 金点下划；红涨绿跌保留。
- **红涨绿跌要覆盖到百分比**：金额与其后的涨跌幅同属一段、同色（首页 hero 用 `▼ -¥1,125 · -0.43%` 的 `·` 分隔式，不用括号）；导航摘要里的收益额与百分比同样着色，其余摘要保持 muted 叙述色。
- 抽屉：22px 顶圆角、32×3 把手、居中文字三键（删除=up/取消=hint/保存=ink+金点）。
- 进度/构成线：2px 高、track 底、实色段、端点 7px 金点。
- 交互契约不许丢：隐私掩码、下拉刷新、左缘右滑返回、页面历史栈、云同步、诊断规则、税率纯手填、「已到账=手动确认」。
- 展示的比例/百分比一律接实时计算链，**不许写死定稿图上的示意值**（定稿图的 58%、+5.7% 都是假数据）。

---

## 施工环境（新会话先看这段，都是踩过的）

- **dev server 用 `dev-alt`(5180)**，不要用默认的 5173——它常被并发会话占着。
  截图脚本默认就打 `http://localhost:5180`，端口对不上会截到空白页。
  launch.json 两个配置都加了 `--strictPort`，端口被占会直接报错而不是静默漂移
  （漂移正是此前预览面板截不到图的原因之一）。

- **源码全是 CRLF**（styles.css / src/*.js / index.html）。
  用 Edit 工具做**跨行**匹配几乎必然失败（old_string 里的 `\n` 对不上文件里的 `\r\n`），
  报错却看不出原因。跨行改动一律写个 node 脚本按行处理：
  ```js
  const s = readFileSync(F, 'utf8');
  const EOL = s.includes('\r\n') ? '\r\n' : '\n';
  const L = s.split(EOL);            // 按行定位、splice 替换，再 join(EOL) 写回
  ```
  单行替换用 Edit 没问题。

- **zen 层写在 styles.css 末尾**，靠后来居上压过前面一万行历史层。
  首页那段（`01-首页 · 按 designs/…/定稿图.html 重排`）是已验收的样板，
  新区块照它写；要显式覆盖哪些属性见上文「覆盖旧层」，此处不重复。

- **抽屉遮罩要去掉模糊**：现有 `.modal-mask` 一类带 `backdrop-filter: blur(...)`，
  而定稿图要求「背景页保持原位**被压暗**」——只有 `rgba` 遮罩，没有毛玻璃。
  03/04/05 及所有抽屉界面施工时一并清掉（styles.css 里搜 `backdrop-filter`）。

- **种子数据是个人仓的 24 家公司**（各 100 股，真实股数不入公开仓）＋上半年 17 笔真实派息标为已到账，
  所以空账本首屏就有真实的「已到账/在途/预估」三段，验配色和比例不必自己造数。
  改过种子后，`.chrome-shot-profile` 里存着的 localStorage 会拿旧状态顶掉新种子——
  出图前先把它的 `bopup-ledger-web-state` 清一次，否则验的不是你刚改的代码。

- **改完先跑 `npm run test:js`**（39 项）。渲染层改动一般不影响，但删算法/改口径时会。

## 施工流程
1. 在 `zen-redesign` 分支施工；**按界面簇推进**（见下方「按界面簇施工」），簇内一界面一 commit（中文信息循仓库风格）；测试全绿（`npm run test:js` / `test:py`；删 XIRR 时同步改 tests/annals.test.mjs 断言到本年收益率口径）。
2. 不动 data/、scripts/（截图脚本除外）、sync/network 管线。
3. **整簇**验收通过后合并 main 推送上线（Pages 只在 push 到 main 时部署）；合并前先 `git fetch` 把行情机器人的数据提交拉进来，避免覆盖。不要只上线宿主页面而把抽屉留在旧版。
4. **上线后立刻回写本文件**：用户确认某簇上线后，由施工该簇的 Agent 更新「当前状态」段、
   簇表里的 ✅ 与目录索引，并消掉该簇已了结的「跨簇依赖」条目。
   这份 README 是唯一权威，不回写下一个会话就会按过时状态开工。

## 验收流程（强制）

**顺序不能反：先出图用眼睛看，再量数值。** 数值只能验证你想到要验的维度，验不出你没想到的——
首页曾出现「17 项样式属性全等、10 个纵向锚点 0px 偏差」却整个 hero 区左对齐的情况。

0. **先起 dev server**：`preview_start` 选配置 `dev-alt`（5180），或 `npm run dev -- --port 5180 --strictPort`。
1. **机检 + 出图**：`npm run verify -- --page=<页>`。全绿才算过红线；退出码非 0 就照它报的修。
   页名用 data-page-nav 值（`home`/`holdings`…），抽屉写 `<名>@modal`（入口表见 verify-ui.mjs 的 MODAL_TARGETS）。
2. **目检**：Read 那两张 PNG，与该文件夹 定稿图.html 并排看，日/夜各一次（夜间对照 00-总则/夜间色板.html）。
   机检管「有没有违规」，人眼管「像不像定稿图」：构图、字阶、留白节奏、金线位置、chevron 一类字符记号，
   以及**该显示的元素有没有被旧层藏掉**——机检查的是违规不是缺席，元素静默消失它抓不到，
   要按定稿图清点「都在不在」。顶部留白要对着状态栏判断时，补一张 `npm run shot -- --shots=<页> --statusbar`。
3. **补语义项**：先过下面「机检盲区」，再过该界面 验收标准.md 的「本界面清单」。
4. 向用户出示截图确认。

### 机检盲区（人工必查，任一违反即不通过）
描边/分隔线/伪元素装饰/按钮底托/水平居中/等比缩放/单屏/横向溢出/掩码破版/tabular-nums
已由 `npm run verify` 自动覆盖，全绿即过，不必手测。以下语义项机器不认识，逐条人工过：
- [ ] 金色只出现在股息与选中记号（机检只认得出旧紫色残留）
- [ ] 红涨绿跌语义正确，**金额后的百分比与金额同色**
- [ ] 隐私掩码开启后金额确实变了掩码（机检只查破版，不查是否掩上）
- [ ] 展示的比例与百分比接实时计算链，未写死定稿图上的示意值
- [ ] 底部动作键落在安全区之上；顶部留白不与状态栏重复（看 --statusbar 那张图）

### 什么算做完（终止条件）
满足这三条就交付，**不要继续精修**：
- `npm run verify` 全绿、`npm run test:js` 全绿；
  **例外**：定稿图本身就是长页的界面（目前只有 10-年度回顾，画布 1150px），
  「单屏不滚动」必然报红，「掩码不破版」也会跟着红（它只是拿页高与视口比）——
  看该项的 `溢出行` 是否为空即可，空就算过，别为凑绿去砍内容；
- 截图与定稿图并排，**肉眼看不出构图/字阶/留白差别**（不追像素级数值相等）；
- 验收标准.md 的本界面清单逐条过。

### 时间预算与止损
- 预算 **每簇 60 分钟** 到可交付，40 分钟处自查一次进度（按实测校准：B 簇 4 界面一次过用了
  60 分钟，其中约 15 分钟耗在自建工具上；C/F 这类小簇应明显更快。60 分钟是上限不是目标）。
  计时到向用户出示截图为止，不含等回复。超预算就先 commit 已完成部分、交阶段截图并说明卡点——
  **宁可如实交一半，不降验收标准赶时间**。
- **产品代码一旦达到可提交状态就先 commit 落袋**，再做补充验证。别攒到最后。
- **禁止为验证新建工具或脚本文件**：验证一律走 `npm run verify` / `npm run shot` 现成入口。
  新抽屉入口不在表里时，在两个脚本的 MODAL_TARGETS 各登记一行、给 verify-ui.mjs 的 关键字号
  加本页两三个代表性选择器（漏了它会报零采样）——这是登记不是造工具。交互后的界面状态用
  `--modal=`/`--nav=` 出图看结果，不写交互测试框架。
  首页曾因自建对照页工具浪费近半时间，B 簇又因自建 157 行交互框架重蹈覆辙。
- 卡在**工具自身**的技术障碍超过 5 分钟，立刻退回手动验证，别修工具。

## 按「界面簇」施工，不要按编号逐个做

18 个界面不是 18 个独立任务。除首页外，每个页面都带着几个**只能从它进入的抽屉**，
抽屉的入口、数据、交互链全长在宿主页面上。拆开做有三个确定的坏处：

- **验不完整**：宿主页的验收标准里就有「点 X 打开 Y」这类条目，抽屉没做就没法验；
- **中间态断裂**：新版页面点开旧版抽屉，两套视觉语言正面撞在一起；
- **重复返工**：抽屉共享同一套通用规格（圆角/把手/遮罩/三键），分批做容易做出三个样。

所以**一簇一做、一簇一验、一簇上线**：

| 簇 | 成员 | 宿主与下钻关系 |
| --- | --- | --- |
| A ✅ | 01-首页 | 已完成上线 |
| B ✅ | **02-持仓页** + 03-持仓详情 + 04-持仓诊断 + 05-编辑抽屉 | 03 从逐股行点名称；04 从页头「诊断 N」或页名下小字行；05 从逐股行的基准股数/税率/每股股息三个可点域 |
| C ✅ | **06-股息日历** + 07-月明细 + 08-股息到账 | 07 从日历月点阵（首页月份点也进 07）；08 从 07 的可点行 |
| D ✅ | **09-收益明细** + 10-年度回顾 + 11-分享卡 + 12-年度回填 | 10 从年度行整行点击；12 从年度行「回填」；11 从 10 的页头「分享」 |
| E ✅ | **13-资金与交易** + 14-记一笔 + 15-交易 + 16-出入金 | 14 从首页「＋记一笔」；15/16 从 14 的选项，也从资金页对应流水行 |
| F | **17-公司基本面** + 18-选择公司 | 18 从基本面页公司名 |

**跨簇依赖（施工时注意）**：
- 已了结：14-记一笔的首页入口、首页净资产 label 进负债弹窗、首页月份点进 07-月明细，
  均已随对应簇上线并回归确认，不必再管。

**簇内推进节奏**：先把宿主页面的构图做出来出一次图给用户确认方向（不是完整验收，只看构图对不对），
再做簇内抽屉，最后整簇一起走验收流程、一起合并上线。

## 目录索引
00-总则（README / 夜间色板.html / 截图与验收.md）
01-首页 ✅ / 02-持仓页 ✅ / 03-持仓详情 ✅ / 04-持仓诊断 ✅ / 05-编辑抽屉 ✅ / 06-股息日历 ✅ / 07-月明细 ✅ / 08-股息到账 ✅ / 09-收益明细 ✅ / 10-年度回顾 ✅ / 11-分享卡 ✅ / 12-年度回填 ✅ / 13-资金与交易 ✅ / 14-记一笔 ✅ / 15-交易 ✅ / 16-出入金 ✅ / 17-公司基本面 / 18-选择公司
